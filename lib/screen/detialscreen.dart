import 'package:flutter/material.dart';

import '../widget/widget.dart';

class detialscreen extends StatelessWidget {
  const detialscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [detialwidget()],
      ),
    );
  }
}
